import math

# Angle in degrees
angle_deg = 60

# Convert degrees to radians
angle_rad = math.radians(angle_deg)

# Calculate sine
result = math.sin(angle_rad)

print(f"sin({angle_deg}°) = {result}")
print(f"sin({angle_deg}°) = {result:.2f}")